{{#each tasks as |task|}}
    {{> task-details}}
{{/each}}
{{!-- <div class="card task">
    <div class="card-head">
        <header>Create a new list</header>
    </div>
    <div class="card-body">
        <form class="form task-create" id="task-create">
            <div class="form-group">
                <label for="task-name">Task Name:</label>
                <input class="form-input" type="text" id="task-name" name="task-name" />
            </div>
            <div class="form-group">
                <button type="submit" value="submit">Submit</button>
            </div>
    </div>

    </form>

</div> --}}